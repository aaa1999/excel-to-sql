# Excel 转 SQL 数据库工具 — 设计方案

> 单机桌面应用（非 Web）。用户在软件中导入 Excel 文件，将其转换为 SQL 数据库。
> 本文档为项目的总体设计与实施计划。

---

## 1. 项目概述

做一个**本地桌面软件**：用户打开软件 → 选择（或拖入）Excel/CSV 文件 → 每个 Sheet 映射为一张表 → 自动推断列类型 → 用户可调整表名、列名、类型、主键 → 一键导入，生成：

1. **SQLite 数据库文件**（`.db`，开箱即用，单机首选）
2. **SQL 脚本文件**（`.sql`，支持 SQLite / MySQL / PostgreSQL 方言，便于导入其他数据库）

全程离线运行，无需安装数据库服务、无需联网。

导入完成后，软件本身即是一个**数据浏览与搜索工作台**：左侧表列表、右侧分页表格；支持按列的**条件搜索**（等于 / 包含 / 不包含，中文直接可用，多条件可组合），每次搜索的结果在**独立的结果标签页**中展示，可保留多个结果页对比，也可导出。

## 2. 需求分析

### 2.1 功能需求

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F1 | 支持导入 `.xlsx` / `.xls` / `.csv`；**可一次选择多个文件，每文件的多个 Sheet 勾选后批量导入** | P0 |
| F2 | Sheet → 数据表：自动以首行作为表头，可手动指定首行是否为表头 | P0 |
| F3 | 列类型自动推断（INTEGER / REAL / TEXT / 日期），用户可逐列修改 | P0 |
| F4 | 导入前预览数据（前 100 行）与最终表结构 | P0 |
| F5 | 可编辑表名、列名，可跳过某列，可指定主键 | P0 |
| F6 | 写入 SQLite 数据库文件（新建或追加到已有库） | P0 |
| F7 | 导出 SQL 脚本（CREATE TABLE + INSERT，多方言） | P1 |
| F8 | 大文件流式处理 + 批量写入，显示进度条与日志 | P1 |
| F9 | 导入完成后显示汇总（表数 / 行数 / 耗时 / 跳过的脏数据） | P1 |
| F10 | 同名表处理策略：覆盖 / 重命名 / 追加（INSERT） | P1 |
| F11 | 直连 MySQL / PostgreSQL 导入（可选，二期） | P2 |
| F12 | 数据浏览：导入后的库在主界面以 表列表 + 分页数据表格 展示 | P0 |
| F13 | 条件搜索：**列下拉框列出当前表的全部列名**（按 Excel 表头原样显示，中文列名直接展示）；操作符 **等于 / 不等于 / 包含 / 不包含 / 局部匹配**（`*` 任意多字符、`?` 单字符），值支持中文；**每个条件的值可用逗号分隔多个**，也可添加多行条件按 且 / 或 组合 | P0 |
| F14 | 搜索结果在**独立的新标签页**展示（命中行数、耗时），可保留多个结果页并排切换对比 | P0 |
| F15 | 搜索结果导出为 Excel / CSV（复用 openpyxl）；可选简单统计图（分组计数条形图） | P2 |

### 2.2 非功能需求

- **单机离线**：所有处理本地完成，不依赖网络与外部数据库服务（SQLite 内嵌）。
- **跨平台**：同时支持 **Windows 10+** 与 **macOS 12+（Intel / Apple Silicon）**，一套代码、双平台分别打包发布（见 5.8 节）。
- **可打包**：最终交付独立可执行文件，目标用户无需安装 Python。
- **健壮性**：脏数据（空行、合并单元格、格式混乱的日期、重复列名）不崩溃，给出明确提示。
- **可测试**：核心引擎与 UI 解耦，引擎可通过 CLI 独立运行与单元测试。

## 3. 技术选型

### 3.1 方案对比

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **Python 3.11 + PySide6** | Excel 生态最成熟（openpyxl）；sqlite3 为标准库；Qt 表格控件强，适合做预览与列配置；PyInstaller 打包简单 | 打包体积较大（约 60–80MB） | ✅ **采用** |
| Python + Tkinter | 无第三方 GUI 依赖 | 表格预览/编辑控件太弱，复杂配置界面难做 | ❌ |
| Electron / Tauri | UI 灵活 | 本质仍是 Web 技术栈，与"不要 Web 形式"的要求相悖 | ❌ |
| Java + JavaFX | 跨平台 | 需捆绑 JRE，Excel 解析生态一般，开发效率低 | ❌ |
| C# + WPF | Windows 体验最佳 | WPF 不支持 macOS | ❌ |

### 3.2 依赖清单

| 用途 | 库 | 说明 |
|------|-----|------|
| GUI | `PySide6` | Qt 官方 Python 绑定，LGPL 许可 |
| 读 Excel | `openpyxl`（xlsx，read_only 流式模式） | 大文件不涨内存 |
| 读 Excel | `xlrd`（旧版 .xls，按需） | 仅 P2，可后置 |
| 读 CSV | 标准库 `csv` | 自动嗅探分隔符与编码（utf-8 / gbk） |
| 写数据库 | 标准库 `sqlite3` | 零依赖 |
| 打包 | `pyinstaller` | 产出 macOS `.app` / Windows `.exe` |

> 不使用 pandas：全量加载 DataFrame 对大文件不友好，openpyxl 流式读取 + sqlite 批量插入更可控、内存占用更低。

## 4. 架构设计

三层架构，**核心引擎不依赖 UI**（可被 GUI 调用，也可命令行直接调用）：

```
┌─────────────────────────────────────────────┐
│  UI 层 (app/ui)          PySide6 界面        │
│  文件选择 / 预览 / 列配置 / 进度与日志        │
├─────────────────────────────────────────────┤
│  业务层 (app/core)        导入引擎           │
│  excel_reader → type_inference → schema      │
│  → sqlite_writer / sql_dumper                │
│  query_builder / db_browser（搜索与浏览）     │
├─────────────────────────────────────────────┤
│  数据层   sqlite3 (标准库) / 文件输出          │
└─────────────────────────────────────────────┘
```

### 核心数据流

```
Excel/CSV 文件
   │ ① 流式逐行读取 (excel_reader)
   ▼
原始行流 ──────► ② 采样前 N 行做类型推断 (type_inference)
   │                    │
   │                    ▼
   │            ③ 生成表结构建议 (schema_builder)
   │                    │
   │            ④ 用户在 UI 中确认/修改（表名、列名、类型、主键、跳过列）
   ▼                    ▼
逐行清洗/转换 ◄──── 最终表结构 (TableMeta)
   │ ⑤ 批量事务写入 (batch = 1000 行)
   ▼
SQLite (.db)  和/或  SQL 脚本 (.sql)
```

### 目录结构

```
excel-to-sql/
├── plan.md
├── requirements.txt
├── main.py                 # GUI 入口
├── cli.py                  # 命令行入口（测试/自动化用）
├── app/
│   ├── __init__.py
│   ├── ui/
│   │   ├── main_window.py      # 主窗口
│   │   ├── pages/
│   │   │   ├── browser_page.py # 主界面：表列表、分页数据表格
│   │   │   ├── search_bar.py   # 搜索条件区（列/操作符/值，多条件组合）
│   │   │   ├── result_tab.py   # 搜索结果标签页（新视图）
│   │   │   ├── file_page.py    # 向导步骤1：选择文件、勾选 Sheet
│   │   │   ├── config_page.py  # 向导步骤2：预览 + 表/列配置
│   │   │   └── run_page.py     # 向导步骤3：进度、日志、结果汇总
│   │   └── widgets/            # 预览表格、列配置表格等自定义控件
│   ├── core/
│   │   ├── excel_reader.py     # 流式读取 xlsx / csv
│   │   ├── type_inference.py   # 列类型推断
│   │   ├── schema_builder.py   # TableMeta 构建、列名清洗
│   │   ├── sqlite_writer.py    # 批量写入 SQLite
│   │   ├── sql_dumper.py       # 生成各方言 SQL 脚本
│   │   ├── query_builder.py    # 条件 → 参数化 SQL（等于/包含/不包含）
│   │   └── db_browser.py       # 读库：分页取数、命中计数、结果导出
│   ├── models/
│   │   ├── table_meta.py       # 表结构定义（表名、列、类型、主键）
│   │   └── column_meta.py      # 列定义
│   └── utils/
│       ├── logger.py
│       └── errors.py           # 统一异常体系
├── tests/                      # pytest 单元测试
├── samples/                    # 示例 Excel（用于开发与测试）
└── docs/
```

## 5. 关键设计

### 5.1 Sheet → 表 的映射规则

- 默认：一个 Sheet 对应一张表，Sheet 名作为表名初始值。
- 表名清洗：非法字符（空格、`-`、`/` 等）→ `_`；以数字开头 → 前缀 `t_`；超长截断；重名自动加后缀 `_2`。所有清洗动作在 UI 上提示用户。
- 首行默认视为表头；用户可关闭（此时列名自动生成为 `col_1, col_2, ...`）。

### 5.2 列名与类型推断

- **列名清洗**：同表名规则；重复列名自动加后缀；空表头 → `col_N`。
- **类型推断**（采样每个 Sheet 前 1000 行非空值，规则从"最宽"到"最窄"）：

  ```
  全部匹配整数        → INTEGER
  全部可转浮点        → REAL
  全部为日期/时间格式  → TEXT(ISO8601)（统一格式化为 YYYY-MM-DD HH:MM:SS）
  其他               → TEXT
  ```

- 空列（全空）→ `TEXT`；混合类型 → 降级为 `TEXT`（保证不丢数据）。
- 用户可对任意列手动覆盖类型；日期列可选择保留原文。

### 5.3 SQLite 类型映射

| 推断类型 | SQLite 类型 |
|----------|-------------|
| INTEGER  | INTEGER     |
| REAL     | REAL        |
| TEXT     | TEXT        |
| 日期      | TEXT（ISO8601，便于排序与比较） |

导出 MySQL/PostgreSQL 脚本时再做方言映射（如 MySQL 的 `TEXT` → `VARCHAR(n)/TEXT`、反引号引用符、批量 INSERT 语法差异）。

### 5.4 大文件与事务

- `openpyxl` 的 `read_only=True` 模式流式逐行读取，内存占用与文件大小无关。
- 写入时每 1000 行一个事务批量 `INSERT`，配合进度回调（已处理行数 / 总行数）。
- CSV 自动嗅探编码（优先 utf-8，失败回退 gbk）与分隔符（`,` `\t` `;`）。

### 5.5 脏数据策略

| 情况 | 处理 |
|------|------|
| 合并单元格（值只在左上角） | 读到 None，写 NULL，预览时提示 |
| 公式单元格 | `read_only` 模式下取缓存计算值；无缓存则写 NULL |
| 数值精度（如身份证、单号被读成浮点/科学计数） | 若列被用户指定为 TEXT，按单元格显示字符串写入；整数超过 2^53 也建议 TEXT |
| 类型转换失败的单元格 | 整列降级 TEXT 或写 NULL（用户可选策略，默认写 NULL 并计数警告） |
| 完全空行 | 跳过 |

### 5.6 同名表冲突策略

导入到已有 `.db` 时：**覆盖**（DROP + CREATE）/ **重命名**（自动 `_new` 后缀）/ **追加**（结构一致则 INSERT，不一致则报错）。默认覆盖，UI 上单选确认。

### 5.7 条件搜索

搜索直接在导入生成的 SQLite 库上执行，SQL 由 `query_builder` 拼装，**值一律参数绑定**，从根上避免 SQL 注入与中文乱码。

| 操作符 | SQL 形式（以列 col 为例） | 说明 |
|--------|--------------------------|------|
| 等于 | `col = ?` | 数值/日期列按原生类型比较；文本列精确匹配 |
| 不等于 | `(col IS NULL OR col != ?)` | 空值视为「不等于」命中；数值列自动按数值比较 |
| 包含 | `CAST(col AS TEXT) LIKE ? ESCAPE '\'`（参数 `%值%`） | CAST 使数值列也能按文本做包含搜索 |
| 不包含 | `(col IS NULL OR CAST(col AS TEXT) NOT LIKE ? ESCAPE '\')` | 空值视为「不包含」命中——否则备注为空的行会从结果中消失，与用户直觉不符 |
| 局部匹配 | `CAST(col AS TEXT) LIKE ? ESCAPE '\'`（参数由用户模式转换） | `*` 匹配任意多字符、`?` 匹配单字符（如 `手机*`、`机?盘`）；未用通配符时按包含处理；字面 `%`/`_` 自动转义 |

- **多值输入**：一个条件的值可用逗号（半角 `,` / 全角 `，`）分隔多个。语义：等于 / 包含 / 局部匹配 → 命中任一（OR）；不等于 / 不包含 → 全部排除 / 全部不含（AND，空值仍命中）。多行条件之间再按全局 且 / 或 组合。
- **通配符转义**：用户输入中的 `%`、`_`、`\` 先转义再作为参数，保证"包含"是字面包含（搜 `100%` 不会误命中 `100x`）。
- **中文**：库内 TEXT 均为 UTF-8，`LIKE` 按字符匹配，中文天然支持；SQLite 的 `LIKE` 大小写不敏感仅对 ASCII 生效，中文无大小写之分，不受影响。
- **多条件**：可添加多行"列 + 操作符 + 值"，条件间全局 且 / 或 切换（任意分组二期再做）。
- **性能**：包含/不包含为全表扫描，单机十万行量级毫秒~百毫秒可接受；结果分页（每页 100 行，`LIMIT/OFFSET`），同时执行一次 `COUNT(*)` 显示命中数与耗时。

### 5.8 跨平台支持（Windows + macOS）

| 事项 | 方案 |
|------|------|
| GUI 框架 | PySide6 官方支持 Windows 与 macOS（含 Apple Silicon arm64），一套代码双平台运行 |
| 打包 | PyInstaller 不支持交叉编译 → 用 GitHub Actions 建 `windows-latest` + `macos-latest` 双平台构建矩阵，分别产出 Windows `.exe` 与 macOS `.app`/dmg |
| 路径 | 全部使用 `pathlib.Path`，不硬编码分隔符；输出目录默认放系统"文档"目录 |
| 中文显示 | Qt 自动使用系统字体（macOS: PingFang SC / Windows: 微软雅黑），无需随包携带字体 |
| 文件编码 | Windows 导出的 CSV 常为 GBK → 读取端做 utf-8 → gbk 嗅探回退（5.4 节）；导出 CSV 时带 UTF-8 BOM，保证双平台用 Excel 打开不乱码 |
| 发布 | macOS 产物做 ad-hoc 签名（避免 Gatekeeper 拦截）；Windows 留意杀软误报，同时提供 zip 绿色版 |

## 6. UI 设计

主界面是**数据浏览与搜索工作台**；导入功能以三步向导形式从主界面唤起。

### 6.1 主界面：数据浏览与搜索

```
┌───────────────────────────────────────────────────────────────┐
│ Excel → SQL   [导入 Excel] [导出 SQL]           当前库: db1.db │
├──────────┬────────────────────────────────────────────────────┤
│ 表列表    │ 🔍 [列▼ 商品名称] [操作符▼ 包含] [值: 手机]        ➕  │
│ orders   │    [列▼ 配送状态] [操作符▼ 不包含] [值: 测试]    ➖  │
│ users    │    组合: (●且 ○或)          [搜索] [重置]          │
│ products │────────────────────────────────────────────────────│
│ ...      │ 标签页: [orders·全部] [搜索结果① 87行] [搜索结果②] │
│          │ ┌─ 数据表格（分页 100 行/页）────────────────────┐  │
│          │ │ 命中 87 / 12,340 行 · 耗时 0.02s              │  │
│          │ │ 订单号 │ 商品名称 │ 金额  │ 下单时间           │  │
│          │ │ 12     │ 手机壳…  │ 29.90 │ 2026-09-01         │  │
│          │ └───────────────────────────────────────────────┘  │
│          │ [导出当前结果: Excel / CSV]                        │
└──────────┴────────────────────────────────────────────────────┘
```

- **搜索条件区的"列"下拉框列出当前表的全部列名**——即 Excel 表头，中文列名原样显示（如 `商品名称`、`订单编号`）；列很多时下拉框支持输入过滤快速定位；
- **结果表格以列名作为表头**，与库中字段一一对应，点击列头可按该列排序；
- 搜索结果固定以**新标签页**呈现（"搜索结果①②…"），与"全部数据"页并存，多次搜索结果可保留、切换对比；
- 结果页顶部显示 命中行数 / 总行数 与耗时，底部可将当前结果导出为 Excel / CSV；
- 空库时主界面显示引导态："点击右上角导入 Excel 开始"。

### 6.2 导入向导（三步）

```
┌──────────────────────────────────────────────────────┐
│ Excel → SQL   [1 选择文件]─[2 配置]─[3 导入]          │
├──────────────────────────────────────────────────────┤
│ 步骤1：拖入或浏览选择文件（支持多选）                   │
│   └─ Sheet 列表：☑ 订单表 (12,340 行)  ☐ 草稿(空)     │
│                                                      │
│ 步骤2：左侧 Sheet 切换 │ 右侧：                        │
│   表名 [orders]  主键 [id ▼]  首行是表头 ☑            │
│   ┌─ 列配置表 ─────────────────────────────┐          │
│   │ 导入☑ │ 列名(可编辑) │ 推断类型(可改▼) │ 示例值    │
│   └────────────────────────────────────────┘          │
│   下方：前 100 行数据预览网格                          │
│                                                      │
│ 步骤3：输出设置（.db 路径 / 是否同时导出 .sql 及方言）  │
│   [开始导入] → 进度条 + 滚动日志                       │
│   完成：✅ 3 张表 / 45,120 行 / 8.2s / 打开输出目录     │
└──────────────────────────────────────────────────────┘
```

导入在 `QThread` 工作线程中执行，避免界面卡死；进度通过信号回传主线程。向导完成后关闭并回到主界面，表列表自动刷新。

## 7. 里程碑计划

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| M1 核心引擎 | excel_reader / type_inference / schema_builder / sqlite_writer + CLI + 单测 | `cli.py` 可将示例 Excel 转出 `.db` |
| M2 基础 GUI | 三步向导骨架：多文件/多 Sheet 勾选、预览、执行导入、进度条 | 能用的导入 GUI |
| M3 浏览与搜索 | 主界面表列表 + 分页浏览；条件搜索（等于/包含/不包含、中文、多条件且/或）；结果新标签页展示；结果导出 Excel/CSV | 浏览搜索完整版 |
| M4 配置增强 | 列名/类型/主键编辑、冲突策略、SQL 脚本导出（3 种方言） | 功能完整版 |
| M5 打包发布 | GitHub Actions 双平台矩阵构建（Windows exe + macOS app/dmg）、macOS 签名与 Windows 误报处理、异常提示打磨、使用文档 | Win / Mac 双平台安装包 |

> **当前进度（2026-09-18）**：M1 已完成（核心引擎 + CLI + 33 项测试全部通过）；M2/M3 GUI 初版已实现——三步导入向导、表列表浏览、条件搜索（列名下拉框展示全部列名）、搜索结果新标签页、结果导出 Excel/CSV，离屏冒烟通过。M4 的列名/类型编辑与冲突策略已随 GUI 初版实现，剩余 SQL 方言脚本导出；M5 打包未开始。

## 8. 测试计划

- **单元测试**（pytest）：类型推断（含脏数据用例）、列名/表名清洗、方言 SQL 生成、CSV 编码嗅探；query_builder 三种操作符、LIKE 通配符转义、中文值参数绑定、多条件且/或组合。
- **集成测试**：`samples/` 下准备典型 Excel（多 Sheet、中文表头、日期列、大数字列、空行列、合并单元格），CLI 跑通后用 sqlite3 校验行数与抽样值；另覆盖中文搜索场景——导入含中文数据后，验证 等于/包含/不包含 的命中行数与预期一致。
- **性能测试**：10 万行 x 20 列的 xlsx，目标 60 秒内完成且内存 < 300MB。
- **GUI 冒烟测试**：手动核对三步流程 + 异常文件（损坏文件、加密文件、空文件）的报错提示。

## 9. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 大 Excel 内存/耗时 | 卡顿、崩溃 | read_only 流式 + 批量事务 + 进度反馈（已纳入 M1 设计） |
| Excel 数据脏、格式多样 | 转换出错或丢数据 | 类型推断保守降级 TEXT + 用户可手动覆盖 + 转换失败计数报告 |
| 旧格式 .xls 兼容 | 部分文件读不了 | M1 先支持 xlsx/csv；.xls 依赖 `xlrd` 放入 P2，读不了时给出明确提示 |
| PyInstaller 打包体积/杀软误报 | 分发体验差 | 用 `--onefile` + 清理无用 Qt 插件；必要时提供 zip 版 |
| 浮点精度丢失（长数字编号） | 数据错误 | 超 2^53 整数自动建议 TEXT 列（5.5 节策略） |
| 「包含/不包含」为全表扫描 | 大表搜索慢 | 单机数据量级可接受；分页展示 + 命中计数；必要时对常搜索的等值列建索引 |
